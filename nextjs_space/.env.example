# Database Configuration (Required)
DATABASE_URL="postgresql://user:password@localhost:5432/traffic_control_plane?schema=public"

# NextAuth Configuration (Required)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# LLM API Configuration (Optional - for AI features)
# Option 1: OpenAI
# OPENAI_API_KEY="sk-..."

# Option 2: Custom OpenAI-compatible provider
# LLM_API_BASE_URL="https://api.openai.com/v1"
# LLM_API_KEY="your-api-key"
# LLM_MODEL="gpt-4o-mini"

# Platform Configuration (Optional)
# Comma-separated list of domains to skip in middleware routing
# PLATFORM_DOMAINS=".example.com,.myplatform.io"
